<Page
    x:Class="Weather.AddLocation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Weather"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <RelativePanel>
        <TextBlock Name="location_header">Add a Location</TextBlock>
        <TextBox x:Name="City" RelativePanel.Below="location_header"></TextBox>
        <ComboBox x:Name="State" RelativePanel.RightOf="City" RelativePanel.Below="location_header"></ComboBox>
        <TextBox Name="Zip" RelativePanel.RightOf="State" RelativePanel.Below="location_header"></TextBox>
        <TextBlock Name="default_label" RelativePanel.Below="City">Make this the city that appears on the home page?</TextBlock>
        <CheckBox x:Name="DefaultCity_Checkbox" RelativePanel.RightOf="default_label" RelativePanel.Below="City"></CheckBox>
        <Button x:Name="AddCity_Button" RelativePanel.Below="default_label">Add City</Button>
    </RelativePanel>
</Page>
